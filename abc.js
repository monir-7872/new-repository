
function sendNotification(email) {
    const emailName = email.split('@');
   for(const part of email){

       if(emailName === '@'){
       console.log()
      }
   }
    }

const email = sendNotification("monir@gmail.com");
console.log(email)